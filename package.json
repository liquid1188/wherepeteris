{
  "name": "wherepeteris",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "build": "npm run fetch && eleventy && pagefind --site _site",
    "build:cached": "eleventy && pagefind --site _site",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0",
    "pagefind": "^1.3.0"
  }
}
