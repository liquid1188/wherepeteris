---
pagination: { data: wpi.authorPages, size: 1, alias: l }
permalink: "{{ l.url }}"
layout: base.njk
eleventyComputed:
  title: "{{ l.item.name }}{% if l.page > 1 %}, page {{ l.page }}{% endif %}"
  description: "Articles by {{ l.item.name }} at Where Peter Is."
---
{% include "listing.njk" %}
